// 클래스 선언
class student {
  constructor(name, age, gender) {
    this.name = name;
    this.age = age;
    this.gender = gender;
  }
}

// 클래스를 이용한 객체 만들기
let a = new student('rosumin', '28', 'male');
console.log(a);
